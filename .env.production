# .env.production - 生产环境变量
# 部署时会替换为实际的 Workers URL
VITE_API_URL=https://simplisave-api.your-subdomain.workers.dev

# SSO 配置（默认不填，独立模式运行；填写后切换为统一认证模式）
# VITE_AUTH_MODE=sso
# VITE_SSO_URL=https://auth.sztcrs.com